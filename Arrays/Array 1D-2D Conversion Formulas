Conversion Formulas:

2D to 1D: c * i + j 
1D to 2D: i = currIndex / c ; j = currIndex % c
where c = total no.of cols, i = rowIndex, j = colIndex.

arr[i] = value at (base addr of arr + (data_type * i)) ; where i -> index // this will fetch the address of arr[i]
